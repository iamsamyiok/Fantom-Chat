# FantomCryptoConfig.cmake.in
# 生成后的 FantomCryptoConfig.cmake 供 find_package(FantomCrypto) 使用
@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/FantomCryptoTargets.cmake")

set(FantomCrypto_VERSION @PROJECT_VERSION@)
check_required_components(FantomCrypto)
